---
name: hifi-to-demo
description: |
  将高保真设计稿或设计描述转换为可运行的前端 Demo，并输出完整的协作文档体系。
  当用户提供 UI 设计（截图、Figma 链接或详细描述）并要求「转成代码」「做前端 Demo」
  「生成可运行原型」时触发此 skill。
  中文触发词：帮我把这个设计转成代码 / 做一个前端demo / 生成可运行的Demo /
  把设计稿变成代码 / 做个可以点击的原型。
  English triggers: "turn this into code", "make a demo", "generate a frontend prototype".
  只要同时存在「设计稿」和「写代码」的意图，无论用户是否明确说 demo，都应调用此 skill。
  如果场景中涉及学习开源代码作为数据来源，同样触发此 skill 并额外生成学习文档。
---

# 高保真设计稿 → 前端 Demo 工作流

本 skill 提炼自 openUBMC Studio L1 协作实践，适用于任何产品领域。
产出不只是一个 Demo 文件，而是支撑团队后续协作的完整文档体系。

---

## 每次调用的标准产出

| # | 文件 | 用途 |
|---|------|------|
| 1 | `design-structure.md` | 布局层级、组件清单（含详细元素描述）、色彩 Token、数据接口 |
| 2 | `interaction-spec.md` | 可交互区域、非静态区域、交互动作分析、**待交互设计师确认清单** |
| 3 | `tech-selection.md` | 技术选型、可行性评估、拓展性分析 |
| 4 | `demo/index.html`（或等价文件） | 含 Mock 数据的可运行代码 |
| 5 | `quality-review.md` | 初步易用性审查报告，列出已修复 / 待修复的问题 |
| 6 | `prompt-asset.md` | 原始 Prompt、已知局限、下一步迭代建议 |
| 7 | `README.md` | 项目说明、目录结构、本地运行方式、协作规范 |
| 8 | `CHANGELOG.md` | 修改日志，按版本记录每次变更 |
| 9 | `BRANCH.md` | 分支管理方案，供团队协作参考 |

**条件产出**（仅在以开源代码为数据来源时生成）：

| # | 文件 | 用途 |
|---|------|------|
| 10 | `learning/[库名].md` | 对参考开源代码的结构拆解与学习笔记，方便后续查阅 |

---

## 工作流

### 第 1 步 — 收集输入

如有不明确的地方主动询问：缺失的色彩 Token、边缘状态的交互行为、数据 Schema。
不要猜测会实质影响实现的内容。

同时确认以下两点：
- 是否涉及参考开源代码？若是，记录库名与参考目的，后续生成 `learning/` 文档。
- 是否为团队协作项目？若是，确认分支命名规范偏好（默认提供通用方案）。

### 第 2 步 — 分析设计 · 结构 + 元素描述

**写任何代码之前**，先完成以下分析并写入 `design-structure.md`：

**2a. 布局层级**
- 页面整体分区（顶栏 / 侧边栏 / 主区 / 底部等）
- 各区域尺寸与间距系统

**2b. 组件清单 + 元素详细描述**
对每个独立 UI 组件逐一描述：
- 组件名称与类型（容器 / 展示 / 输入 / 导航等）
- 视觉细节：背景色、边框、圆角、阴影、字体规格
- 内部元素的层叠关系（哪些元素叠在哪些元素上方）
- 与相邻组件的间距关系
- 空状态 / 加载状态下的外观（若设计稿有体现）

**2c. 色彩 Token**
提取所有使用的 hex 值，优先 hex 而非颜色名。

**2d. 数据接口**
每个组件由什么数据驱动？定义 TypeScript 风格的接口。

**2e. 非静态区域标注**
识别设计稿中哪些区域不是静态图：
- 动态数据区（列表、图表、实时数值）
- 加载中 / 骨架屏区域
- 条件渲染区（登录态 / 权限控制 / 空状态）
- 动画 / 过渡区域

若用户明确说「先别急着写代码」「先给我看分析」，**在此停止**，呈现分析后等待确认。

### 第 3 步 — 交互分析 + 可行性评估（合并）

将交互分析与可行性检查合并完成，写入 `interaction-spec.md`。

**3a. 可交互区域与交互动作分析**
对每个可交互元素：
- 交互触发方式（点击 / 悬停 / 拖拽 / 滚动 / 输入 / 手势）
- 触发后的系统响应（页面跳转 / 状态变更 / 弹层 / 展开折叠 / 数据更新）
- 完整状态枚举：default / hover / active / disabled / loading / error / empty / success
- 逆操作路径（用户如何撤销或返回）

**3b. 待交互设计师确认清单**
以下内容从设计稿无法确定，需交互设计师补充或确认后方可实现：
- 在 `interaction-spec.md` 末尾单独列出「⚠️ 待设计师确认」区块
- 典型项目：动画时长与缓动曲线、错误提示文案与出现时机、边缘状态（空数据 / 超长文本 / 权限不足）的视觉处理方式、多步骤流程的回退规则

**3c. 可行性与拓展性评估**
对每个交互动作评估：
- 当前 Demo 阶段能否实现（完整实现 / Mock 实现 / 仅占位）
- 接入真实数据后是否需要重构
- 是否存在影响后续拓展的架构决策（如状态管理方式、事件总线、组件通信模式）

标记以下需要澄清的高风险项：
- 复杂数据可视化（图布局、实时流）
- 需要物理引擎或 Canvas 的动画
- 平台特定 API（摄像头、文件系统、原生手势）

### 第 4 步 — 技术选型

按产品类型选型：

| 产品类型 | 推荐技术栈 |
|---------|-----------|
| 图 / 拓扑可视化 | Vue 3 + ReactFlow 或 Cytoscape.js + ELK/dagre |
| 数据密集型 Dashboard | React + shadcn/ui + Recharts |
| 表单密集型企业工具 | Vue 3 + Element Plus |
| 移动端消费者应用 | Plain HTML+CSS+JS 或 React mobile-first Tailwind |
| SaaS 管理后台 | React + Tailwind CSS |

**利益相关者审查 Demo 始终优先单文件、零安装方案。** 目标是一个双击就能在浏览器打开的文件，而不是需要 `npm install` 的项目。

在 `tech-selection.md` 中记录选型理由、被排除的方案及原因、迁移路径。

### 第 5 步 — 生成代码

分层构建：
1. 静态 HTML 结构（正确的语义元素）
2. 应用样式（颜色 / 字体 / 间距与设计稿对齐）
3. 添加交互（状态变更、过渡动画、事件处理器）
4. 接入 Mock 数据（字段命名真实，表格至少 5–8 条记录）

### 第 6 步 — 初步易用性审查

代码生成后，在提交给用户之前先自查，将结果写入 `quality-review.md`。

**审查维度：**

| 维度 | 检查项 |
|------|--------|
| 可读性 | 文字对比度是否足够（深色背景配浅色文字 / 反之）；字号是否过小（正文 < 12px 即警告） |
| 层级清晰度 | 主要操作按钮与次要按钮是否有明显视觉权重差异；标题与正文是否可区分 |
| 交互反馈 | 可点击元素是否有 hover / active 状态；按钮 disabled 时是否视觉上明显不可点 |
| 布局稳定性 | 动态内容填入后是否会引起意外布局抖动；长文本是否有截断或换行处理 |
| 空状态 | 列表 / 图表为空时是否有提示，而非空白区域 |
| 错误状态 | 表单错误提示是否清晰、位置是否靠近出错字段 |

对每个问题标注：
- ✅ 已在 Demo 中修复
- ⚠️ 存在但不影响核心流程（说明原因）
- ❌ 需设计师重新定义（说明为什么无法在 Demo 层修复）

### 第 7 步 — 生成协作文档

**README.md**
```markdown
# [项目名称]

## 项目说明
[一句话描述这个 Demo 的目的和覆盖范围]

## 目录结构
[列出所有产出文件及其用途]

## 本地运行
[运行方式，如：直接在浏览器打开 demo/index.html]

## 设计稿来源
[Figma 链接 / 截图位置]

## 当前版本覆盖范围
[列出已实现的功能和已知未实现项]

## 待设计师确认
[从 interaction-spec.md 摘取的待确认清单]

## 协作规范
[指向 BRANCH.md]
```

**CHANGELOG.md**
```markdown
# 修改日志

## [v0.1.0] - [日期]
### 新增
- 初始 Demo 生成

### 已知问题
- [列出 quality-review.md 中的 ⚠️ 和 ❌ 项]
```

**BRANCH.md**
```markdown
# 分支管理方案

## 主分支
- `main`：稳定版本，只接受 PR 合并
- `dev`：日常开发分支

## 功能分支命名规范
- `feat/[功能描述]`：新功能
- `fix/[问题描述]`：问题修复
- `design/[版本或模块]`：设计稿更新对应的实现

## PR 规范
- PR 标题格式：`[类型] 简短描述`
- 合并前需更新 CHANGELOG.md

## 当前分支建议
[根据项目实际情况给出具体分支建议]
```

### 第 8 步（条件）— 开源代码学习文档

**仅在场景中使用开源代码作为数据来源或参考实现时执行。**

为每个参考的开源库生成 `learning/[库名].md`，内容包括：

```markdown
# [库名] 学习笔记

## 为什么参考这个库
[本次使用的目的]

## 核心概念
[该库解决什么问题，核心抽象是什么]

## 关键 API / 用法
[本次实际用到的部分，带代码示例]

## 目录结构拆解
[参考了哪些文件，各文件的作用]

## 与本项目的适配点
[如何将库的能力映射到当前 Demo 的需求]

## 值得深入的部分
[哪些能力本次没用到但后续可能用到]

## 参考资料
[官方文档链接 / 相关示例链接]
```

---

## 来自 openUBMC Studio 的实践笔记

**Hex 优于颜色名。** 始终从设计稿提取精确 hex 值。`#141420` 无歧义；「深海军蓝」则不然。

**图布局必须用布局引擎。** 永远不要硬编码图节点的 x/y 坐标——哪怕只有 5 个节点也会变成维护噩梦。使用 ELK、dagre 或 ReactFlow 自动布局。若无法通过 npm 获取布局引擎，改用按层编程定位（如 `computeLayout()` 按层分组），并记录升级路径。

**Mock 数据 = API 契约。** Mock 数据的字段形状应与真实后端返回保持一致。字段命名如 `lastLoginAt`（而非 `last_login`）应匹配约定的 API 规范，这样 Demo 可直接用于 API 契约协商。

**暂停并确认。** 当用户要求先看分析再写代码时，这是工作流信号——他们想在投入前验证理解。要真正停下来呈现结构化分析，而不是一边「确认」一边继续写代码。

**交互设计师确认是关口，不是可选项。** `interaction-spec.md` 中的「待确认」清单不是礼貌性提问，是真正的实现前置条件。没有确认就写的交互细节大概率要返工。

**文档是协作界面。** README / CHANGELOG / BRANCH 不是形式主义——它们是设计侧产出与开发侧接收之间的协议文件。写清楚比写漂亮更重要。

---

## Artifact 模板

### design-structure.md

```markdown
# [产品名称] · 设计结构文档

## 页面层级
[用 ASCII 树描述布局层级]

## 组件清单与元素描述

### [组件名]
- 类型：[容器 / 展示 / 输入 / 导航]
- 视觉：背景 #XXXXXX，圆角 Xpx，阴影 ...
- 内容层叠：[描述内部元素叠放关系]
- 间距：与上方组件间距 Xpx，内边距 Xpx
- 空状态外观：[描述]

## 非静态区域
| 区域 | 类型 | 说明 |
|------|------|------|
| ...  | 动态数据 / 条件渲染 / 动画 | ... |

## 色彩 Token
| Token | 值      | 用途 |
|-------|---------|------|
| ...   | #XXXXXX | ...  |

## 数据接口
```typescript
interface Entity {
  id: string
  // ...
}
```

## 数据流
[描述数据如何在页面中流动]
```

### interaction-spec.md

```markdown
# [产品名称] · 交互细化说明

## 可交互区域与交互动作

### [组件名]
| 触发方式 | 系统响应 | 状态变化 | Demo 实现程度 |
|---------|---------|---------|-------------|
| 点击    | ...     | ...     | 完整 / Mock / 占位 |

## 关键交互序列
### [交互名称]
1. 用户操作
2. 系统响应
3. 状态变化
4. 逆操作路径

## 可行性与拓展性评估
| 功能 | 当前可行性 | 拓展风险 | 说明 |
|------|-----------|---------|------|
| ...  | ✅ 完整实现 / ⚠️ 有限制 / ❌ 不可行 | 低 / 中 / 高 | ... |

## ⚠️ 待交互设计师确认
以下内容从设计稿无法确定，请设计师补充后方可进入开发：

1. [ ] [动画时长与缓动曲线]
2. [ ] [错误提示文案与出现时机]
3. [ ] [边缘状态视觉处理：空数据 / 超长文本 / 权限不足]
4. [ ] [多步骤流程的回退规则]
```

### quality-review.md

```markdown
# [产品名称] · 易用性审查报告

## 审查结论
[一句话总结：整体可用 / 存在若干需关注问题 / 存在影响核心流程的问题]

## 问题清单

| # | 维度 | 问题描述 | 状态 | 说明 |
|---|------|---------|------|------|
| 1 | 可读性 | ... | ✅ 已修复 / ⚠️ 已知 / ❌ 需设计师介入 | ... |

## 未覆盖的审查项
[列出因 Demo 性质无法验证的项目，如真实数据量下的性能表现]
```
